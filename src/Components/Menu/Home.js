import { Grid } from "@mui/material";

const Home = () => {
    return (
        <Grid>
            <Grid>Picture</Grid>
            <Grid>About me</Grid>
        </Grid>
    );
};

export default Home;
